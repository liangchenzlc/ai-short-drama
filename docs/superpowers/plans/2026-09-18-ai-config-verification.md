# AI 配置接口联调结果

验证日期：2026-09-18。

## 自动检查

- 后端：`python scripts/run_integration.py tests -q -p no:cacheprovider --tb=short`，193 passed、2 skipped。跳过项为未显式开启的 MinIO 集成测试；本轮更改没有涉及存储适配器。
- Ruff：`ruff check .`、`ruff format --check .` 均通过，123 个 Python 文件格式合规。
- 前端：`npm run typecheck`、`npm run build` 均通过。
- API 测试覆盖三类 CRUD、分页、字符串 ID/版本、加密密钥保留与清除、软删除、404/409/422、默认切换及校验响应不回显密钥。

## 真实浏览器

使用 Playwright CLI，经 Vite 5174 → FastAPI 18000 → 独立云端临时 MySQL 库验证。原业务数据库未写入测试配置。

- 文本、生图、生视频配置分别通过页面新增，POST 返回 201；刷新后持久化成功。
- 三类配置修改成功，PATCH 返回 200；密钥输入不回填，留空保持原值。
- 三类默认分别设置成功，PUT 返回 200；默认值互不影响。
- 显式清除密钥生效；空 Base URL 可保存；停用配置的默认操作被禁用。
- 模拟保存 503 时表单保留输入，恢复后可保存；实际并发修改触发 409，表单保留输入并要求主动重新加载版本。
- 模拟列表连接中断，页面提示错误，恢复后重新加载成功。
- 三类配置通过确认弹窗删除，DELETE 返回 204；刷新后总数为 0。
- 桌面 1440×960、手机 390×844 截图位于 `frontend/output/playwright/ai-config-desktop.png` 与 `ai-config-mobile.png`。手机页面无整页水平溢出，表格可内部横向滚动。
- 控制台仅有测试主动触发的 503、409 和连接中断错误，无其他运行时错误。

浏览器辅助脚本位于 `frontend/output/playwright/ai-*-check.js`，是本次隔离环境中的分步验证记录；不要直接在业务数据库运行写入脚本。测试过程中修正了辅助脚本 URL 处理以适配 CLI 环境，应用请求本身未受影响。

## 审查与最终环境

独立代码审查发现并修复：表单错误地强制填写可选 Base URL，以及停用配置仍能点击设置默认。同步修正 AI 页面侧栏的数据保存说明。

临时测试后端、Vite 进程和本次创建的临时数据库均已清理。正常开发环境已启动：前端 `http://127.0.0.1:5173/ai`，后端 `http://127.0.0.1:8000/docs`。后端直接查询和前端代理查询均返回 HTTP 200，响应符合分页结构。

本次验证为配置管理数据链路，不包含调用第三方模型生成内容。
