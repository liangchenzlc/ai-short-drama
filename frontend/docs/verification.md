# 验证记录

## AI 模型分类精简（2026-09-18）

- `npm run build` 通过；仅保留文本模型、生图模型、生视频模型三个标签。
- 三类配置的新增、编辑、删除及默认配置隔离通过，新增表单自动采用当前分类。
- 空状态、方向键切换、跨页面导航保留当前分类通过；0 pageerrors。
- 1440px 桌面与 390px 手机布局检查通过，手机页面无横向溢出，宽表在容器内滚动。
- 已移除 AI 配置中的高级设置、生成设置、SD2 资产管理及其他服务类型代码。

## 网页布局与配色改造（2026-09-18）

- 最终 `npm run build` 通过；浏览器控制台 0 errors / 0 warnings，外部请求数 0。
- 检查 1440 × 960 与 390 × 844 的项目、详情、制作和配置布局。
- 编辑剧本 → 切到素材 → 返回剧本，文本保持；测试后恢复原文。
- 首页「继续创作」直接进入对应分集。
- 手机素材页：页面宽度 390px，内容宽度 390px；素材面板及内部内容宽度均为 356px。
- 点击底部下一步进入分镜，当前步骤自动横向滚动到可见区域。
- 主按钮白字/琥珀底对比度 5.31:1，次要文字/页面底 4.96:1，侧栏文字/深色底 9.22:1。
- 独立截图复核确认：新增素材块溢出、预览宽度、当前步骤可见性、残留蓝色链接和模型名称均已修复。
- Impeccable 命令未返回有效检测报告；采用源码、计算值、浏览器交互和独立截图审查验证。

## 初次迁移

2026-09-18，本机 Chrome / Playwright CLI。

- `npm run build`：通过 TypeScript 检查与 Vite 生产构建，产物为 `dist/`。
- 桌面 1440 × 1000：项目管理、项目详情、分集制作、素材库、AI 配置显示正常。
- 手机 390 × 844：剧本编辑及展开分镜表单正常换行；页面宽度与内容宽度均为 390px。
- 打开演示项目、进入分集、生成剧本候选、生成文本框架：成功，显示“已保存”。
- 新建项目并刷新：项目仍显示在最近项目中。
- 素材搜索：无匹配时显示空状态。
- AI 配置和生成设置可切换；导航切换时组件保持挂载。
- 最终浏览器控制台：0 errors、0 warnings；当前页面资源记录中外部请求数为 0。
- 源码检查：未包含 Electron、desktop bridge、后端请求或自定义媒体协议。
- 独立静态与截图复核：未发现重大迁移或布局问题。

截图保存在 `output/playwright/`（不提交）。本轮未实际导入用户媒体文件；IndexedDB 导入实现通过类型检查与静态审查。AI 生成、视频渲染与后端服务不在本项目范围内。

## 路由与分镜重构（2026-09-18）

- React Router 覆盖项目、分集、制作步骤、素材分类和 AI 配置；浏览器跳转、深层链接刷新及前进后退通过。
- 1920 × 1080：分集侧栏左坐标为 0；分镜脚本、分镜图、分镜视频三栏顶部对齐。
- 390 × 844：页面宽度与内容宽度均为 390px，三栏纵向排列；窗口缩小时当前步骤自动移入可见区域。
- 第一步没有上一步按钮；最后一步没有底部返回详情按钮，保留顶部返回入口。
- 分镜只显示编号；图片和视频提示词编辑区已移除。脚本修改、刷新恢复通过，测试后恢复原文。
- 无效项目和分集展示提示，无效制作步骤回退至有效步骤；验证期间没有 pageerror。
- 静态托管需要 SPA fallback，已记录于 README。
